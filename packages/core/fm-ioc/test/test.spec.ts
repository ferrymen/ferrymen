import { expect } from 'chai';
import { Mock } from './fixture/mock';

// tslint:disable:no-unused-expression
describe('TDD', () => {
  it('should test success', () => {
    expect(Mock).not.undefined;
  });
});
